{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LendingDetail list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Invested</th>
                <th>Dateinvested</th>
                <th>Transactionid</th>
                <th>Createdat</th>
                <th>Modifiedat</th>
                <th>Modifiedby</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lendingdetail_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.invested }}</td>
                <td>{% if entity.dateInvested %}{{ entity.dateInvested|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.transactionId }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.modifiedAt %}{{ entity.modifiedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.modifiedBy }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lendingdetail_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lendingdetail_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lendingdetail_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
