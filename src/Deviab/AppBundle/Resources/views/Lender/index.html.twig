{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Lender list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dateinvested</th>
                <th>Transactionid</th>
                <th>Invested</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lender_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateInvested %}{{ entity.dateInvested|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.transactionId }}</td>
                <td>{{ entity.invested }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lender_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lender_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lender_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
